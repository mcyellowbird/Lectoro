name: Build and Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          npm install -D tailwindcss
          npm install flowbite
          npm install -D tailwindcss-3d
          npx tailwindcss -i src/css/base.css -o src/css/index.css

      - name: Deploy
        uses: mcyellowbird/github-pages-deploy-action@v4
        with:
          folder: Main
